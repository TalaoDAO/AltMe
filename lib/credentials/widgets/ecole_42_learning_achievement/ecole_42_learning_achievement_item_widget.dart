import 'package:flutter/material.dart';

class Ecole42LearningAchievementItemWidget extends StatelessWidget {
  const Ecole42LearningAchievementItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display Loyalty card');
  }
}
