import 'package:flutter/material.dart';

class CertificateOfEmploymentItemWidget extends StatelessWidget {
  const CertificateOfEmploymentItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
