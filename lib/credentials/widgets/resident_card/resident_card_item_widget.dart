import 'package:flutter/material.dart';

class ResidentCardItemWidget extends StatelessWidget {
  const ResidentCardItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
