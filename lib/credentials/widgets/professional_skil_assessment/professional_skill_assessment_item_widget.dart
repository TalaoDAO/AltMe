import 'package:flutter/material.dart';

class ProfessionalSkillAssessmentItemWidget extends StatelessWidget {
  const ProfessionalSkillAssessmentItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
