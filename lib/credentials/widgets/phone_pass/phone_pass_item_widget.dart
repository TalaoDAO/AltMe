import 'package:flutter/material.dart';

class PhonePassItemWidget extends StatelessWidget {
  const PhonePassItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
