import 'package:flutter/material.dart';

class ProfessionalExperienceAssessmentItemWidget extends StatelessWidget {
  const ProfessionalExperienceAssessmentItemWidget({Key? key})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
