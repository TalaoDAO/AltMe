import 'package:flutter/material.dart';

class LearningAchievementItemWidget extends StatelessWidget {
  const LearningAchievementItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list certificate');
  }
}
