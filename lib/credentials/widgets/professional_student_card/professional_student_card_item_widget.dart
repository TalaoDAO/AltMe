import 'package:flutter/material.dart';

class ProfessionalStudentCardItemWidget extends StatelessWidget {
  const ProfessionalStudentCardItemWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('display list identity');
  }
}
